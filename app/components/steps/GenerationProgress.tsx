import { Platform } from "@pipeline/Types";
import { Platforms } from "@app/Platforms";

interface Props {
    platform: Platform | null;
}

const GenerationProgress = ({ platform }: Props) => <div>asdsadasd</div>;

export default GenerationProgress;
